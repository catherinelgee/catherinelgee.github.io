// have a grid that is true/false (filled or not filled)
// if the whole line is full then get rid of the line
//
// TODO DAS?? 10 wide 20 high

// needs: opening screen (choose level)
// then play screen needs: points (todo lookup)
// longterm maybe get some stats lol

// 60 fps

var grid = [];

for (var i = 0;i < 20;i++) {
     grid[i] = [];
}



function drawPiece() {

}

function drawGrid() {
// draws the output of the grid (?)
}

function createWorld() {
    // fills the grid with false values
}

function drawOpening() {
    // draws the opening title
}

function draw() {
    // the actual game
    drawOpening();

}
